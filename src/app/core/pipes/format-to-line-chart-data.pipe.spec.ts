import { FormatToLineChartDataPipe } from './format-to-line-chart-data.pipe';

describe('FormatToLineChartDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatToLineChartDataPipe();
    expect(pipe).toBeTruthy();
  });
});
