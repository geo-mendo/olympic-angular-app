import { ToPieDataFormatPipe } from './to-pie-data-format.pipe';

describe('ToPieDataFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ToPieDataFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
